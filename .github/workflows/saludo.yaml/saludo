name: Nombre del Workflow

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  nombre-del-job:
    runs-on: ubuntu-latest

    steps:
      - name: Paso 1
        uses: actions/checkout@v4

      - name: Paso 2
        run: echo "Hola desde el runner"
